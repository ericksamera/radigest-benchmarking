# Data requirements

Reference FASTA files are produced by the references workflow in a later stage and are not committed.

Empirical BAM/CRAM inputs are optional until public inputs are available and `config/scenarios/reviewer_empirical.yml` marks them as required.
